import { FaSyncAlt } from 'react-icons/fa';
import ToolbarButton from '../ToolbarButton';

export const Update = () => {
  return (
    <ToolbarButton>
      <FaSyncAlt />
    </ToolbarButton>
  );
};
