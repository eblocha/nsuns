import { FaUser } from 'react-icons/fa';
import ToolbarButton from '../ToolbarButton';

const Profile = () => {
  return (
    <ToolbarButton>
      <FaUser />
    </ToolbarButton>
  );
};

export default Profile;
