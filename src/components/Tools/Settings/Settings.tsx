import { FaSlidersH } from 'react-icons/fa';
import ToolbarButton from '../ToolbarButton';

const Settings = () => {
  return (
    <ToolbarButton>
      <FaSlidersH />
    </ToolbarButton>
  );
};

export default Settings;
