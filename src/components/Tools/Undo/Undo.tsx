import { FaUndoAlt } from 'react-icons/fa';
import ToolbarButton from '../ToolbarButton';

const Undo = () => {
  return (
    <ToolbarButton>
      <FaUndoAlt />
    </ToolbarButton>
  );
};

export default Undo;
